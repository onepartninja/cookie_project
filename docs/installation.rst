.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install cookie_project

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cookie_project
    $ pip install cookie_project
